const button=document.getElementById('mobileMenuButton'),menu=document.getElementById('mainMenu');function closeMenu(){menu.classList.remove('active');button.setAttribute('aria-expanded','false');button.setAttribute('aria-label','Menüyü aç')}button.addEventListener('click',()=>{const open=menu.classList.toggle('active');button.setAttribute('aria-expanded',String(open));button.setAttribute('aria-label',open?'Menüyü kapat':'Menüyü aç')});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu.classList.contains('active')){closeMenu();button.focus()}});document.addEventListener('click',e=>{if(!e.target.closest('header'))closeMenu()});window.matchMedia('(min-width:861px)').addEventListener('change',closeMenu);(()=>{const hero=document.querySelector('.city-hero');if(!hero)return;const slides=[...hero.querySelectorAll('.hero-slide')],dots=[...hero.querySelectorAll('[data-slide]')],controls=hero.querySelector('.hero-controls');let current=0;const show=i=>{current=(i+slides.length)%slides.length;slides.forEach((s,j)=>s.hidden=j!==current);dots.forEach((d,j)=>d.setAttribute('aria-pressed',String(j===current)));hero.querySelector('.slide-status').textContent=(current+1)+' / '+slides.length};controls.hidden=false;hero.querySelector('.hero-prev').addEventListener('click',()=>show(current-1));hero.querySelector('.hero-next').addEventListener('click',()=>show(current+1));dots.forEach(d=>d.addEventListener('click',()=>show(Number(d.dataset.slide))));hero.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key==='ArrowLeft'){e.preventDefault();show(current+(e.key==='ArrowRight'?1:-1))}});let start=null;hero.addEventListener('pointerdown',e=>{if(e.target.closest('a,button,input'))return;start={x:e.clientX,y:e.clientY}});hero.addEventListener('pointerup',e=>{if(!start)return;const x=e.clientX-start.x,y=e.clientY-start.y;start=null;if(Math.abs(x)>60&&Math.abs(x)>Math.abs(y)*1.5)show(current+(x<0?1:-1))});hero.addEventListener('pointercancel',()=>start=null)})();
