const button=document.getElementById('mobileMenuButton'),menu=document.getElementById('mainMenu');function closeMenu(){menu.classList.remove('active');button.setAttribute('aria-expanded','false');button.setAttribute('aria-label','Menüyü aç')}button.addEventListener('click',()=>{const open=menu.classList.toggle('active');button.setAttribute('aria-expanded',String(open));button.setAttribute('aria-label',open?'Menüyü kapat':'Menüyü aç')});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu.classList.contains('active')){closeMenu();button.focus()}});document.addEventListener('click',e=>{if(!e.target.closest('header'))closeMenu()});window.matchMedia('(min-width:861px)').addEventListener('change',closeMenu);(()=>{const hero=document.querySelector('.city-hero');if(!hero)return;const slides=[...hero.querySelectorAll('.hero-slide')],controls=hero.querySelector('.hero-controls'),play=hero.querySelector('.hero-play'),reduced=matchMedia('(prefers-reduced-motion: reduce)');let current=0,timer=null,playing=!reduced.matches,hover=false,focus=false;const show=i=>{current=(i+slides.length)%slides.length;slides.forEach((s,j)=>s.hidden=j!==current)};const sync=()=>{clearInterval(timer);timer=null;play.textContent=playing?'Ⅱ':'▶';play.setAttribute('aria-label',playing?'Otomatik geçişi durdur':'Otomatik geçişi başlat');if(playing&&!hover&&!focus&&!document.hidden)timer=setInterval(()=>show(current+1),6000)};controls.hidden=false;hero.querySelector('.hero-prev').addEventListener('click',()=>{show(current-1);sync()});hero.querySelector('.hero-next').addEventListener('click',()=>{show(current+1);sync()});play.addEventListener('click',()=>{playing=!playing;sync()});hero.addEventListener('mouseenter',()=>{hover=true;sync()});hero.addEventListener('mouseleave',()=>{hover=false;sync()});hero.addEventListener('focusin',()=>{focus=true;sync()});hero.addEventListener('focusout',e=>{if(!hero.contains(e.relatedTarget)){focus=false;sync()}});hero.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key==='ArrowLeft'){e.preventDefault();show(current+(e.key==='ArrowRight'?1:-1));sync()}});let start=null;hero.addEventListener('pointerdown',e=>{if(e.target.closest('a,button,input'))return;start={x:e.clientX,y:e.clientY}});hero.addEventListener('pointerup',e=>{if(!start)return;const x=e.clientX-start.x,y=e.clientY-start.y;start=null;if(Math.abs(x)>60&&Math.abs(x)>Math.abs(y)*1.5){show(current+(x<0?1:-1));sync()}});hero.addEventListener('pointercancel',()=>start=null);document.addEventListener('visibilitychange',sync);reduced.addEventListener('change',()=>{playing=!reduced.matches;sync()});sync()})();
